meta {
  name: HEAD livez
  type: http
  seq: 3
  tags: [
    health
    https
  ]
}

head {
  url: {{httpsUrl}}/livez
  body: none
  auth: inherit
}

tests {
  test("HEAD livez returns 204", function () {
    expect(res.status).to.equal(204);
  });
}
